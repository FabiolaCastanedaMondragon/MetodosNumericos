#include <stdio.h> //Bibliotecas (library) studio--standar input-output h--> Permite arrastrar datos a la pantalla
#include <stdlib.h>

int main (){ //funcion principal
    printf ("Hello word"); // el punto y coma indica que se llavara lo que esta antes del ; a la memoria RAM
    system("Pause");
    return 0;
}